diff --git a/node_modules/stryker-mutator-bun-runner/.bun-tag-98d8da5e5a3ac5a3 b/.bun-tag-98d8da5e5a3ac5a3
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/BunTestAdapter.js b/dist/BunTestAdapter.js
index b2f145efd9fa834e4ddbc2d071f6d056df475bc7..9d997b84ed560ebbb32e8f42fad6c54e1ec0f7be 100644
--- a/dist/BunTestAdapter.js
+++ b/dist/BunTestAdapter.js
@@ -1,8 +1,22 @@
 import { execa } from 'execa';
 import { glob } from 'glob';
+import { readFile, unlink } from 'node:fs/promises';
+import * as os from 'node:os';
+import * as nodePath from 'node:path';
 import { BunResultParser } from './BunResultParser.js';
 import { MutantCoverageCollector, CoverageHookGenerator } from "./coverage/index.js";
 import { ProcessPoolSingleton } from './process/ProcessPoolSingleton.js';
+// --- DEAD-TESTS PATCH: junit-based result parsing (bun 1.3.x emits no per-test console lines when piped) ---
+let __junitCounter = 0;
+function __getAttr(attrs, key) {
+    const m = new RegExp(`${key}="([^"]*)"`).exec(attrs || '');
+    return m ? m[1] : '';
+}
+function __decodeEntities(s) {
+    return (s || '')
+        .replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&#39;/g, "'")
+        .replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&amp;/g, '&');
+}
 class TimeoutError extends Error {
     timedOut = true;
     constructor(message) {
@@ -68,6 +82,7 @@ export class BunTestAdapter {
      * @throws Error if test execution fails or times out
      */
     async runTests(testFiles, runOptions) {
+        this.junitPath = nodePath.join(os.tmpdir(), `stryker-bun-junit-${process.pid}-${++__junitCounter}.xml`);
         const args = await this.buildBunArgs(testFiles, runOptions);
         const env = this.buildEnvironment(runOptions);
         const coverageTracker = this.initializeCoverage(runOptions);
@@ -144,13 +159,78 @@ export class BunTestAdapter {
         }
     }
     async parseResults(execResult) {
-        const outputToParse = execResult.stderr || execResult.stdout;
-        return this.parser.parse(outputToParse);
+        let xml;
+        try {
+            xml = await readFile(this.junitPath, 'utf-8');
+        }
+        catch (e) {
+            this.log.warn(`Could not read JUnit report at ${this.junitPath}: ${e}. Falling back to console parse.`);
+            const outputToParse = execResult.stderr || execResult.stdout;
+            return this.parser.parse(outputToParse);
+        }
+        try {
+            await unlink(this.junitPath);
+        }
+        catch { /* ignore */ }
+        return this.parseJUnit(xml);
+    }
+    parseJUnit(xml) {
+        const tests = [];
+        let passed = 0, failed = 0, skipped = 0;
+        const caseRe = /<testcase\b([^>]*?)(\/>|>([\s\S]*?)<\/testcase>)/g;
+        let m;
+        while ((m = caseRe.exec(xml)) !== null) {
+            const attrs = m[1];
+            const inner = m[3] || '';
+            const name = __decodeEntities(__getAttr(attrs, 'name'));
+            const file = __decodeEntities(__getAttr(attrs, 'file'));
+            const lineNo = __getAttr(attrs, 'line');
+            const time = parseFloat(__getAttr(attrs, 'time') || '0');
+            const id = file ? `${file}::${name}` : name;
+            let status = 'passed';
+            let error;
+            if (/<failure\b/.test(inner) || /<error\b/.test(inner)) {
+                status = 'failed';
+                const fm = /<(?:failure|error)\b([^>]*)>/.exec(inner);
+                error = fm ? __decodeEntities(__getAttr(fm[1], 'message') || '') || 'Test failed' : 'Test failed';
+            }
+            else if (/<skipped\b/.test(inner)) {
+                status = 'skipped';
+            }
+            const t = { id, name, file, line: lineNo ? parseInt(lineNo, 10) : undefined, status, duration: time * 1000 };
+            if (error)
+                t.error = error;
+            tests.push(t);
+            if (status === 'passed')
+                passed++;
+            else if (status === 'failed')
+                failed++;
+            else
+                skipped++;
+        }
+        this.log.debug(`Parsed JUnit: ${tests.length} tests (${passed} passed, ${failed} failed, ${skipped} skipped)`);
+        return {
+            tests,
+            passed,
+            failed,
+            total: tests.length,
+            duration: undefined,
+            failedTests: tests.filter(t => t.status === 'failed')
+        };
     }
-    attachCoverageResults(result, coverageTracker) {
+    async attachCoverageResults(result, coverageTracker) {
         if (coverageTracker.enabled) {
-            const coverageResult = this.coverageCollector.stopCoverage();
-            result.coverage = coverageResult;
+            // DEAD-TESTS PATCH: read the mutantCoverage the child dumped at exit.
+            const outPath = this.coverageHookGenerator.coverageOutPath;
+            try {
+                const raw = await readFile(outPath, "utf-8");
+                result.coverage = { mutantCoverage: JSON.parse(raw) };
+                try { await unlink(outPath); } catch { /* ignore */ }
+            }
+            catch (e) {
+                this.log.warn(`Could not read mutant coverage at ${outPath}: ${e}`);
+                result.coverage = { mutantCoverage: { static: {}, perTest: {} } };
+            }
         }
         return result;
     }
@@ -211,9 +291,15 @@ export class BunTestAdapter {
         this.addCoverageArgs(args, options);
         this.addTestOptions(args, options);
         this.addNodeArgs(args);
+        this.addJUnitReporter(args);
         await this.addTestFiles(args, testFiles);
         return args;
     }
+    addJUnitReporter(args) {
+        if (this.options.command)
+            return;
+        args.push('--reporter=junit', `--reporter-outfile=${this.junitPath}`);
+    }
     addBaseCommand(args) {
         if (this.options.command) {
             const commandParts = this.options.command.split(' ');
diff --git a/dist/BunTestRunner.js b/dist/BunTestRunner.js
index 8c21e725f250fe84347a35e36ffecb4f3bc3d6bf..13797d951131faf1d80033cd1ed927604a52d425 100644
--- a/dist/BunTestRunner.js
+++ b/dist/BunTestRunner.js
@@ -129,7 +129,9 @@ export class BunTestRunner {
             const baseProps = {
                 id: test.id || index.toString(),
                 name: test.name,
-                timeSpentMs: test.duration || 0
+                timeSpentMs: test.duration || 0,
+                ...(test.file ? { fileName: test.file } : {}),
+                ...(test.line ? { startPosition: { line: Math.max(0, test.line - 1), column: 0 } } : {})
             };
             switch (test.status) {
                 case 'passed':
@@ -151,11 +153,12 @@ export class BunTestRunner {
         return { status: DryRunStatus.Complete, tests };
     }
     processCoverageData(coverage, result) {
-        this.log.debug('Processing coverage data');
-        const mutantCoverage = this.bunAdapter.getCoverageCollector().toMutantCoverage(coverage.coverage);
+        // DEAD-TESTS PATCH: coverage.mutantCoverage is the {static, perTest} object
+        // dumped from the bun child — already in Stryker's MutantCoverage shape.
+        const mutantCoverage = coverage.mutantCoverage || { static: {}, perTest: {} };
         result.mutantCoverage = mutantCoverage;
         this.mutantCoverage = mutantCoverage;
-        this.log.debug(`Collected coverage for ${Object.keys(mutantCoverage.perTest).length} tests`);
+        this.log.debug(`Collected coverage: ${Object.keys(mutantCoverage.static || {}).length} static mutant(s), ${Object.keys(mutantCoverage.perTest || {}).length} perTest`);
     }
     handleDryRunError(error, options) {
         this.log.error('Dry run failed', error);
@@ -170,7 +173,7 @@ export class BunTestRunner {
     createMutantRunOptions(options) {
         return {
             timeout: options.timeout,
-            bail: true,
+            bail: false,
             activeMutant: parseInt(options.activeMutant.id, 10),
             env: { __STRYKER_ACTIVE_MUTANT__: options.activeMutant.id.toString() }
         };
diff --git a/dist/coverage/CoverageHookGenerator.js b/dist/coverage/CoverageHookGenerator.js
index 2d33d0bc87361a84ab86716aa3409fb81140d685..a1abbb2eed5908509aa46f5b394af15f26207bc6 100644
--- a/dist/coverage/CoverageHookGenerator.js
+++ b/dist/coverage/CoverageHookGenerator.js
@@ -6,13 +6,17 @@ import * as fs from 'fs/promises';
  */
 export class CoverageHookGenerator {
     coverageHookPath;
+    coverageOutPath; // DEAD-TESTS PATCH: where the child dumps __stryker__.mutantCoverage
     /**
      * Creates a coverage hook file that will be preloaded by Bun.
      * @returns Promise that resolves to the path of the created hook file
      */
     async createHookFile() {
-        const hookContent = this.generateHookContent();
-        await this.writeHookFile(hookContent);
+        const tempDir = path.join(process.cwd(), '.stryker-tmp');
+        await fs.mkdir(tempDir, { recursive: true });
+        this.coverageHookPath = path.join(tempDir, 'coverage-hook.js');
+        this.coverageOutPath = path.join(tempDir, 'mutant-coverage.json');
+        await fs.writeFile(this.coverageHookPath, this.generateHookContent(this.coverageOutPath), 'utf-8');
         return this.coverageHookPath;
     }
     /**
@@ -33,14 +37,27 @@ export class CoverageHookGenerator {
      * Generates the complete JavaScript content for the coverage hook.
      * @returns JavaScript code that sets up coverage tracking
      */
-    generateHookContent() {
-        return `
-// Stryker Bun Coverage Hook
-// This file is automatically generated and injected to collect coverage data
-
-${this.getInitializationCode()}
-${this.getMutantTrackingCode()}
-${this.getTestWrapperCode()}
+    generateHookContent(outPath) {
+        // DEAD-TESTS PATCH: Stryker's instrumented source populates
+        // globalThis.__stryker__.mutantCoverage in the bun child. The runner
+        // process can't read that global across the process boundary, so we
+        // dump it to a file on exit and the adapter reads it back. With
+        // coverageAnalysis "all" (no currentTestId set) the instrumenter writes
+        // everything to mutantCoverage.static — exactly what we need.
+        // Bun's `bun test` does NOT fire process "exit"/"beforeExit" in preloads,
+        // so we dump from a global afterAll (registered in a preload, it runs for
+        // every test file; the last write carries the cumulative coverage).
+        return `// Stryker Bun Coverage Hook (patched)
+import { afterAll } from "bun:test";
+import { writeFileSync } from "node:fs";
+const __STRYKER_COVERAGE_OUT__ = ${JSON.stringify(outPath)};
+if (typeof globalThis.__stryker__ === "undefined") { globalThis.__stryker__ = {}; }
+afterAll(() => {
+  try {
+    const ns = globalThis.__stryker__ || {};
+    writeFileSync(__STRYKER_COVERAGE_OUT__, JSON.stringify(ns.mutantCoverage || { static: {}, perTest: {} }));
+  } catch (e) { /* ignore */ }
+});
 `;
     }
     /**
